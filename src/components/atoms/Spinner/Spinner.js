import React from 'react';

import { Wrapper, Circle } from './Spinner.style';

const Spinner = () => {
  return (
    <Wrapper>
      <Circle />
      <Circle />
      <Circle />
    </Wrapper>
  );
};

export default Spinner;
